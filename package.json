{
  "name": "xats-setup",
  "version": "0.3.5",
  "description": "One-command, OS-agnostic installer for the xats cross-agent bus (daemon + service + Claude Code/Pi/OpenCode integration + bridge). Windows, macOS, Linux.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Abdk4Moura/xats-setup.git"
  },
  "bin": {
    "xats-setup": "install.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/**/*.test.mjs"
  },
  "files": [
    "install.mjs",
    "lib/",
    "assets/",
    "README.md"
  ],
  "keywords": [
    "xats",
    "cross-agent-teams",
    "installer",
    "cross-platform"
  ],
  "license": "MIT"
}
